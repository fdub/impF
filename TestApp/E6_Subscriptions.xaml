<Window x:Class="TestApp.E6_Subscriptions"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LoggingV" Height="300" Width="300"
        xmlns:vm="clr-namespace:Example6_Subscriptions;assembly=TestVm"
        d:DataContext="{d:DesignInstance vm:Subscriptions, IsDesignTimeCreatable=False}">
    <TextBlock FontSize="64" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Run Text="{Binding Seconds.V, Mode=OneWay}" /><Run Text="." /><Run Text="{Binding Tenth.V, Mode=OneWay}" />
    </TextBlock>
</Window>
